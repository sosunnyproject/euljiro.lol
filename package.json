{
  "scripts": {
    "start": "parcel -p 3000 index.html --open",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://github.com/sosunnyproject/threejs-euljiro.git",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "dat.gui": "^0.7.7",
    "file-system": "^2.2.2",
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0",
    "three": "^0.133.1"
  }
}
